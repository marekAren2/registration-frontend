const config = {
    api: {
        // zmien swoj backend bo react frontend chodzi na "3000"
        url : "http://localhost:3005"
    }
}

// pamietaj w react export - ujemy default nie component to male litery nazwy?
export default config ;